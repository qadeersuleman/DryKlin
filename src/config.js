const config = {
    botName: "ChatBot",
    initialMessages: [
      { type: "text", text: "Hello! How can I help you?" },
    ],
    // Add more configurations as needed
  };
  
  export default config;
  